mkdir -p ${1:$DEV_ROOT/dir}
cd $1/

cmake .. -G "$OME_MAKE" \
      -DCMAKE_INSTALL_PREFIX=$HOME/.local \
      -DCMAKE_EXPORT_COMPILE_COMMANDS=1 \
      -DCMAKE_CXX_STANDARD=11
cmake --build . --use-stderr --config Release --target install