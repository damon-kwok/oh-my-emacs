using System.Collections;
using UnityEngine;
using UnityEngine.UI;

public class ${1:UI_Template} : NinjaBehaviour {

    Button ${2:button__return} = null;
    Button ${3:button__replay} = null;
	$0
    void Start()
    {
        this.$2 = this.transform.Find("$2").GetComponent<Button>();
        this.$3 = this.transform.Find("$3").GetComponent<Button>();
        
        UGUI_EventListener.Get($2.gameObject).onClick = OnButtonClick;
        UGUI_EventListener.Get($3.gameObject).onClick = OnButtonClick;
    }
    
    private void OnButtonClick(GameObject go)
    {
        Debug.Log("Button OnClick:" + go.name);
        
        if (go == this.$2.gameObject)
        {
            
        }else if (go == this.$3.gameObject)
        {
            
        }
        
    }
}
